# 🧱 الگوی طراحی نما (Facade Pattern)
## 🎯 هدف الگو
الگوی نما (Facade) یکی از الگوهای طراحی ساختاری (Structural) است که یک اینترفیس ساده و یکپارچه را برای مجموعه پیچیده‌ای از کلاس‌ها (که به آن‌ها زیرسیستم یا Subsystem گفته می‌شود) فراهم می‌کند.

💡 هدف اصلی این الگو این است که پیچیدگی زیرسیستم را از دید کلاینت پنهان کند و کار با آن را آسان سازد. Facade مثل یک کنترل از راه دور عمل می‌کند که با چند دکمه، مجموعه‌ای از عملیات پیچیده را پشت صحنه مدیریت می‌کند.

## 🧠 چطور این الگو را عمیق یاد بگیریم؟
برای یادگیری مؤثر این الگو، پیشنهاد می‌شود مراحل زیر را دنبال کنید 👇

ابتدا فایل Facade-fa.pdf را مرور کنید تا با مفهوم، دلایل استفاده و ساختار کلی آشنا شوید.

سپس فایل main.py را بررسی و اجرا کنید تا منطق ایجاد API ساده و مدیریت کلاس‌های زیرسیستم را مشاهده کنید.

در نهایت وارد پوشه‌ی real-sample/ شوید و نمونه‌کدهای واقعی را مطالعه کنید تا ببینید در چه پروژه‌هایی و چگونه از Facade استفاده می‌شود.

## 🧩 توضیح ساختار و منطق کد main.py
### 🔹 کلاس‌های زیرسیستم (Subsystem1, Subsystem2)
این کلاس‌ها نماینده‌ی بخش‌های پیچیده و تخصصی سیستم هستند.

آن‌ها می‌توانند عملیات‌های مختلفی داشته باشند (مانند operation1, operation_n, operation_z).

این کلاس‌ها هیچ اطلاعی از Facade ندارند و از دید آن‌ها، Facade فقط یک کلاینت دیگر است.

### 🔹 کلاس اصلی Facade
این کلاس ورودی ساده‌شده به زیرسیستم‌هاست و هسته‌ی الگو را تشکیل می‌دهد.

ویژگی‌ها:

مدیریت وابستگی: Facade ارجاعی به یک یا چند کلاس زیرسیستم نگهداری می‌کند (در متد __init__).

اینترفیس ساده (operation): این متد یک میان‌بر مناسب برای اجرای چندین کار پیچیده در زیرسیستم‌ها به ترتیب درست است.

```bash
def operation(self) -> str:
    # فراخوانی متدهای متوالی در زیرسیستم‌ها به ترتیب منطقی
    # ...
```
پنهان‌سازی: Facade ترتیب فراخوانی‌ها، وابستگی‌ها و منطق درونی زیرسیستم‌ها را کاملاً از دید کلاینت پنهان می‌کند.

### 🔹 کد سمت کلاینت (client_code)
کد کلاینت برای انجام یک عملیات سطح بالا فقط Facade را فراخوانی می‌کند و نیازی به آشنایی با صدها متد و کلاس داخلی زیرسیستم‌ها ندارد. این یعنی اصل سادگی و کاهش وابستگی رعایت شده است.

#### 🧰 خروجی اجرای برنامه (output.txt)
خروجی  نشان می‌دهد که Facade چگونه عملیات پیچیده‌ی آماده‌سازی و اجرا در زیرسیستم‌ها را در پشت یک فراخوانی ساده پنهان کرده است:

## 🚀 تمرین پیشنهادی واقعی (Real Practice)
### 🔧 تمرین ۱ – سیستم پخش فیلم (Home Theater)
یک HomeTheaterFacade بسازید که با فراخوانی متد watchMovie(movie_name)، تمام زیرسیستم‌های Projector، Amplifier و DvdPlayer را روشن، تنظیم و پخش را آغاز کند.
### 🔧 تمرین ۲ – سیستم تبدیل ویدیو
یک VideoConverterFacade طراحی کنید که متد convert(filename, format) را پیاده‌سازی کند و تمام منطق پیچیده‌ی خواندن فایل، ایجاد کدک‌ها و بهینه‌سازی (با استفاده از زیرسیستم‌های داخلی) را در پشت خود پنهان کند.

### 🔧 تمرین ۳ – مدیریت عملیات دیتابیس
یک DatabaseFacade بسازید که متد ساده‌ی saveUser(data) را داشته باشد و این متد خود شامل عملیات پیچیده‌ی زیر باشد: باز کردن اتصال، شروع تراکنش، اینسرت، و بستن تراکنش.

## منابع پیشنهادی برای مطالعه بیشتر
🌐 Refactoring.Guru – Facade Pattern

📘 Head First Design Patterns (فصل Facade)

🎥 YouTube درباره کاربرد Facade در پروژه‌های واقعی