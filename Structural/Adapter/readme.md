


**Adapter Pattern** is a structural design pattern that allows incompatible interfaces to work together.  
It acts as a *translator* between two classes that can’t directly communicate.

Use it when you want to reuse an existing class but its interface doesn’t match the one you need.

<div dir="rtl">

# 🔌 الگوی طراحی آداپتر (Adapter Pattern)

الگوی آداپتر یکی از **الگوهای ساختاری (Structural Patterns)** است که هدفش **سازگار کردن دو کلاس ناسازگار** است تا بتوانند بدون تغییر در کدهای اصلی با هم کار کنند.  
در واقع، آداپتر نقش **مترجم** بین دو سیستم یا کلاس را دارد.



## 🎯 هدف

فرض کنید یک کلاس دارید که رفتار مفیدی دارد اما رابط (interface) آن با سیستم فعلی‌تان سازگار نیست.  
به جای بازنویسی کل سیستم، می‌توانید با استفاده از یک **Adapter** آن را به زبان سیستم خودتان ترجمه کنید.


## 🧩 ساختار کلاس‌ها

در این پیاده‌سازی سه بخش اصلی داریم:

- **Target** → رابط اصلی که کلاینت از آن استفاده می‌کند.  
- **Adaptee** → کلاسی که رفتار مورد نیاز را دارد، اما رابطش متفاوت است.  
- **Adapter** → پلی بین Target و Adaptee که درخواست‌ها را ترجمه می‌کند.




## 🧠 مسیر پیشنهادی برای یادگیری

1. ابتدا **فایل slides.pdf** را بخوان تا مفهوم را به‌صورت تصویری درک کنی.  
2. سپس فایل **main.py** را بررسی و اجرا کن تا با نحوه‌ی عملکرد Adapter آشنا شوی.  
3. در پایان، وارد پوشه‌ی **real_samples/** شو تا نمونه‌های واقعی از پروژه‌ها را ببینی.  
   اگر نمونه‌ی واقعی داری، لطفاً **Fork** کن و در این پوشه اضافه‌اش کن 🙌



## 💡 مثال ساده در این پروژه

در این مثال، کلاس `Adaptee` متدی به نام `specific_request` دارد که با ساختار مورد نیاز `Target` سازگار نیست.  
کلاس `Adapter` این ناسازگاری را رفع کرده و به کلاینت اجازه می‌دهد بدون تغییر در کلاس‌های موجود، از هر دو استفاده کند.

## 📘 فایل main.py — توضیحات کد

فایل main.py رفتار آداپتر را در یک سناریوی واقعی شبیه‌سازی می‌کند 👇

🔹 کلاس Target

کلاسی است که رابط اصلی سیستم را نشان می‌دهد.
کد کلاینت (client) فقط این رابط را می‌شناسد و با آن کار می‌کند.

🔹 کلاس Adaptee

کلاسی است که رفتار مفیدی دارد اما رابطش متفاوت است.
یعنی نمی‌توان آن را مستقیماً به client_code داد.

🔹کلاس  Adapter

کلاسی که نقش مترجم را بازی می‌کند.
از هر دو کلاس (Target و Adaptee) ارث‌بری کرده و رابط‌ها را با هم سازگار می‌کند.

🔹متد  client_code()

تابعی است که فقط با رابط Target کار می‌کند.
این تابع نشان می‌دهد که کد کلاینت بدون تغییر، می‌تواند با Adapter هم کار کند.

🔹 اجرای اصلی (__main__)

در این بخش، سه حالت بررسی می‌شود:

1-کار با Target به‌صورت عادی ✅

2- کار با Adaptee که ناسازگار است ❌

3- کار با Adapter که مشکل را حل می‌کند ✅


### خروجی اجرای برنامه (output.txt)

## 🧭 تمرین پیشنهادی (Real Practice)

در یک پروژه واقعی، فرض کن سیستم پرداخت جدیدی داری که فقط متد process_payment(amount) را می‌شناسد،
اما سیستم قدیمی شرکت فقط متد make_transaction(value) دارد.

به‌جای بازنویسی کل سیستم، یک Adapter بنویس که تماس‌های process_payment() را به make_transaction() تبدیل کند.
به این ترتیب، بدون تغییر در کدهای موجود، هر دو سیستم با هم سازگار می‌شوند — و این دقیقاً همان اصل باز-بسته (Open/Closed Principle) است. ♻️

## منابع پیشنهادی

Refactoring.Guru — Adapter Pattern

Design Patterns: Elements of Reusable Object-Oriented Software

