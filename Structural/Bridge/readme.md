<div dir="rtl">

# 🌉 الگوی طراحی پل (Bridge Pattern)

الگوی **Bridge** یکی از الگوهای مهم **ساختاری (Structural Patterns)** است که به شما کمک می‌کند **منطق سطح بالا (Abstraction)** را از **جزئیات سطح پایین (Implementation)** جدا کنید.  
این جداسازی باعث می‌شود هر دو بخش بتوانند **به‌صورت مستقل توسعه یابند** و بدون ایجاد تغییرات دست‌و‌پاگیر، سیستم را گسترش دهید.



## 🎯 هدف

الگوی Bridge زمانی مفید است که:

-* کلاس‌ها بیش از حد بزرگ شده‌اند  
-* چندین نسخه از یک کلاس قرار است ساخته شود  
-* پیاده‌سازی‌ها در سیستم‌های مختلف باید یکسان مدیریت شوند  
-* نیاز به توسعه مستقل Abstraction و Implementation داریم  

این الگو  کمک می‌کند:

✔ وابستگی بین لایه‌ها کم شود  
✔ تغییرات هر بخش روی بخش دیگر اثر نگذارد  
✔ کد تمیز، گسترش‌پذیر و قابل‌نگهداری شود  


## 🧩 ساختار الگوی Bridge در این پروژه

این پروژه شامل چهار بخش اصلی است:

###1️⃣  کلاس **Abstraction**
کلاسی که عملیات سطح بالا را تعریف می‌کند و یک شیء Implementation را نگه می‌دارد.  
تمام کار واقعی را به Implementation واگذار می‌کند.

### 2️⃣ کلاس **ExtendedAbstraction**
نسخه‌ی پیشرفته‌ی Abstraction که رفتار اضافی ارائه می‌دهد.  
بدون تغییر در Implementation می‌توان نسخه‌های جدید اضافه کرد.

### 3️⃣ کلاس **Implementation (Interface)**
رابطی که تمام پیاده‌سازی‌های واقعی باید آن را رعایت کنند.  
این لایه معمولاً عملیات سطح پایین را ارائه می‌دهد.

### 4️⃣ کلاس **ConcreteImplementationA / ConcreteImplementationB**
دو نسخه‌ی واقعی از Implementation که هرکدام رفتار مخصوص خود را دارند  
(مثلاً اجرای عملیات روی پلتفرم A یا B).



## 📘 توضیح فایل main.py

در فایل `main.py` عناصر اصلی الگوی Bridge به شکل زیر پیاده‌سازی شده‌اند:

### 🔹 کلاس Abstraction  
-* یک implementation را دریافت می‌کند  
-* عملیات اصلی را اجرا می‌کند  
-* بخش اصلی منطق high-level است  

### 🔹 کلاس ExtendedAbstraction  
-* Abstraction را گسترش می‌دهد  
-* رفتار پیشرفته‌تر ارائه می‌دهد  

### 🔹 رابط Implementation  
-* متد `operation_implementation()` را تعریف می‌کند  
-* بستری منعطف برای پیاده‌سازی‌های مختلف ایجاد می‌کند  

### 🔹 پیاده‌سازی‌های Concrete  
-* `ConcreteImplementationA` و `ConcreteImplementationB`  
-* عملیات را بر اساس نوع پلتفرم اجرا می‌کنند  

### 🔹 تابع client_code  
-* فقط Abstraction را می‌شناسد  
-* وابسته به نوع Implementation نیست  
-* هر ترکیبی از Abstraction + Implementation را می‌تواند اجرا کند  

### 🔹 اجرای اصلی (__main__)
در اجرای برنامه:

1- کلاس Abstraction با ImplementationA ترکیب می‌شود  
2- کلاس ExtendedAbstraction با ImplementationB ترکیب می‌شود  



### 🧪خروجی اجرای برنامه (output.txt)

## 🧭 تمرین پیشنهادی (Real Practice)

برای درک بهتر الگوی Bridge، یک سناریوی واقعی را پیاده‌سازی کن:

### 🎨 سناریو: سیستم رندرینگ اجزای UI

فرض کن می‌خواهی یک کتابخانه UI بسازی که کامپوننت‌های مختلفی دارد:

-* **Button**
-* **Card**


و می‌خواهی این کامپوننت‌ها بتوانند روی چند پلتفرم مختلف رندر شوند:

-* 📱 WebGL  

-* 🖥️ OpenGL

-* 🎮 DirectX

### اگر بدون Bridge طراحی کنی:

-* برای هر کامپوننت × هر پلتفرم  
-* باید یک کلاس جدا بسازی  
-* یعنی تعداد کلاس‌ها *منفجر* می‌شود 😅  
  (کابوس نگهداری…!)

### ✔ راه‌حل با Bridge

- **Abstraction** → کامپوننت‌ها (Button, Card, ...)
- **Implementation** → موتورهای رندرینگ (WebGLRenderer, OpenGLRenderer, ...)

در نتیجه:

-* بدون دست‌زدن به کد دکمه، می‌توانی موتور رندرینگ را تغییر دهی  
-* بدون دست‌زدن به موتور رندرینگ، می‌توانی Card یا Modal جدید اضافه کنی  



## منابع پیشنهادی 
Refactoring.Guru — Adapter Pattern

Design Patterns: Elements of Reusable Object-Oriented Software
