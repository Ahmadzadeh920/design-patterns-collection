# 🎯 هدف الگو

الگوی Decorator یک الگوی ساختاری (Structural) است که به شما اجازه می‌دهد رفتار جدیدی را به اشیا اضافه کنید بدون اینکه کد کلاس اصلی را تغییر دهید.
در این الگو، اشیا در داخل Wrapper (دکوراتور) قرار می‌گیرند و رفتار اضافی به صورت پویا (Runtime) به آن‌ها تزریق می‌شود.

## هدف اصلی الگو:
✔️ افزودن رفتار به شکل انعطاف‌پذیر

✔️ جلوگیری از ایجاد زیرکلاس‌های متعدد

✔️ رعایت Open/Closed Principle (باز برای توسعه، بسته برای تغییر)

##  چطور این الگو را عمیق یاد بگیریم؟

برای یادگیری مؤثر، توصیه می‌شود مراحل زیر را دنبال کنید:

1- ابتدا اسلاید آموزشی (decorator-fa.pdf) را مطالعه کنید تا ساختار و انگیزه الگو را درک کنید.

2- سپس فایل main.py را اجرا کنید تا رفتار لایه‌گذاری (wrapping) را ببینید.

3- در پایان وارد پوشه real-sample/ شوید و مثال‌های واقعی Decorator 
را بررسی کنید (مثل Middleware یا Notifier Chain).

# 🧩 توضیح ساختار و منطق کد main.py
### 🔹 کلاس پایه Component

این کلاس نقش اینترفیس مشترک بین همهٔ اشیا (کامپوننت اصلی و Decorator ها) را دارد.

دارای تنها یک متد است:
```bash 
def operation(self) -> str:
    pass
```

این متد همان نقطه‌ای است که قابلیت تزئین (decorate) روی آن انجام می‌شود.

### 🔹 کلاس ConcreteComponent
این کلاس رفتار اصلی / اولیه را پیاده‌سازی می‌کند:
```bash

def operation(self) -> str:
    return "ConcreteComponent"
```
این همان شیء خام است که قرار است بعداً تزئین (wrap) شود.

### 🔹 کلاس Decorator
این کلاس پایهٔ تمام دکوراتورها است.

ویژگی‌ها:

1- رابط مشترک با Component دارد

2- یک Component دیگر را در خود نگه می‌دارد

3- همهٔ کار را به شیء داخل خودش واگذار (delegate) می‌کند

4- نقطهٔ اصلی برای ایجاد دکوراتورهای جدید است
```bash 
def operation(self) -> str:
    return self._component.operation()
```

### 🔹 کلاس‌های دکوراتور واقعی
🟦 ConcreteDecoratorA
این دکوراتور رفتار شیٔ اصلی را تغییر می‌دهد یا گسترش می‌دهد:
```bash
return f"ConcreteDecoratorA({self.component.operation()})"
```
#### 🟩 ConcreteDecoratorB
این دکوراتور یک لایه دیگر روی نتیجهٔ A یا خود کامپوننت اضافه می‌کند:
```bash
return f"ConcreteDecoratorB({self.component.operation()})"

```
#### 🎯 نکته: هر Decorator می‌تواند Decorator قبلی را wrap کند → ایجاد زنجیرهٔ رفتار

### 🔹 کد سمت کلاینت – client_code
این کد فقط با Component Interface کار می‌کند:

```bash
print(f"RESULT: {component.operation()}")
```
بنابراین:

✔️ نمی‌فهمد با دکوراتور کار می‌کند یا کامپوننت ساده

✔️ اصل Open/Closed رعایت می‌شود

✔️ سیستم قابل توسعه است بدون تغییر کد کلاینت

#### 🧰 خروجی اجرای برنامه (output.txt)
این خروجی نشان می‌دهد:

شی اصلی → ConcreteComponent

دکوراتور A یک لایه اضافه می‌کند

دکوراتور B روی A قرار می‌گیرد

نتیجه لایه‌گذاری ترکیبی:
```bash 
ConcreteDecoratorB(ConcreteDecoratorA(ConcreteComponent))
```
این همان مفهوم Wrapping است.

# 🚀 تمرین‌های پیشنهادی واقعی (Real Practice)
## 🔧 تمرین ۱ – سیستم اعلان (Notifier)

ساختار زیر را با Decorator پیاده‌سازی کنید:

EmailNotifier → کامپوننت اصلی

SMSDecorator → ارسال SMS

SlackDecorator → ارسال پیام Slack

در نهایت:
یک پیام را ارسال کنید که همهٔ کانال‌ها آن را دریافت کنند.

## 🔧 تمرین ۲ – Middleware در وب‌سرور

در یک سیستم ساده FastAPI:

Request → Component

LoggingMiddleware → Decorator

AuthMiddleware → Decorator

ThrottleMiddleware → Decorator

در نهایت زنجیرهٔ Middleware بسازید.

## 🔧 تمرین ۳ – پردازش فایل

شیء پایه:

FileReader → Component

Decoratorها:

BufferedReader → افزودن buffer

EncryptedReader → رمزگشایی

CompressedReader → unzip

یک فایل را با چند لایه Decorator پردازش کنید.

# 📘 خلاصهٔ مفاهیم کلیدی اسلایدها (decorator-fa.pdf)

پس از مطالعه اسلایدها، به نکات زیر توجه کنید:

Decorator = Wrapper

رفتار جدید را در زمان اجرا اضافه می‌کند

از ساخت زیرکلاس‌های زیاد جلوگیری می‌کند

هر Decorator همان اینترفیس کامپوننت را دارد

Decoratorها می‌توانند روی هم قرار بگیرند (Stacking)

موارد کاربرد عملی:

Middleware

Notifier Chain

I/O Streams

UI Styling

# 📚 منابع پیشنهادی

🌐 Refactoring.Guru – Decorator Pattern

📘 Head First Design Patterns – فصل Decorator

🎥 YouTube – مثال‌های واقعی Decorator در پروژه‌های Backend