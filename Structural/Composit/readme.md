# 🧱 الگوی طراحی کامپوزیت (Composite Pattern)

## 🎯 هدف الگو  
الگوی **کامپوزیت** یک الگوی ساختاری (Structural) است که اجازه می‌دهد **اشیاء ساده و اشیاء پیچیده (دارای فرزند)** را در یک **ساختار درختی** مدل‌سازی کنیم و سپس با آن‌ها **مثل یک شیء واحد** رفتار کنیم.  
هدف این الگو این است که «جزء» و «کل» از دید کلاینت یکسان دیده شوند.


## 🧠 چطور این الگو را عمیق یاد بگیریم؟

برای یادگیری مؤثر این الگو، پیشنهاد می‌شود مراحل زیر را دنبال کنید 👇

1- ابتدا **اسلاید آموزشی (slides)** را مرور کنید تا با مفهوم و ساختار کلی آشنا شوید.  
2- سپس فایل **`main.py`** را بررسی و اجرا کنید تا منطق ساخت درخت و رفتار یکپارچه را مشاهده کنید.  
3- در نهایت وارد پوشه‌ی **`real-sample/`** شوید و نمونه‌کدهای واقعی را مطالعه کنید تا ببینید در چه پروژه‌هایی و چگونه از Composite استفاده می‌شود.



## 🧩 توضیح ساختار و منطق کد `main.py`

### 🔹 کلاس پایه `Component`  
این کلاس نقش **پایه‌ی مشترک** بین Leaf و Composite را دارد و شامل موارد زیر است:

-* متدهای مدیریت درخت: `()add` و `()remove`  
-* تشخیص اینکه آیا یک جزء قابل ترکیب است: `()is_composite`  
-* متد انتزاعی مشترک: `()operation`  
-* قابلیت داشتن **parent** برای ایجاد ساختار درختی



### 🔹 کلاس `Leaf`  
نماینده‌ی **گره‌های نهایی درخت** است.  
فرزند ندارد و فقط عملیات مربوط به خود را انجام می‌دهد:

```python
def operation(self) -> str:
    return "Leaf"
```
### 🔹 کلاس Composite

نماینده‌ی اجزای پیچیده / قابل ترکیب است که فرزند دارند.

ویژگی‌ها:

-* نگهداری فرزندان در لیست

-* پیاده‌سازی متدهای add() و remove()

-* بازنویسی is_composite() به True

اجرای بازگشتی operation() روی تمام فرزندان:

```

return f"Branch({'+'.join(results)})"

```
این قسمت ستون اصلی رفتار درختی Composite است.

### 🔹 کد سمت کلاینت (client_code)

کد کلاینت بدون اینکه بداند با Leaf کار می‌کند یا Composite، تنها از اینترفیس Component استفاده می‌کند.
این یعنی اصل "یکنواختی" رعایت شده است.

### 🧰 خروجی اجرای برنامه (output.txt)


این خروجی نشان می‌دهد:

ساختار درختی کاملاً پیمایش می‌شود . Composite نتیجهٔ فرزندان را تجمیع می‌کند. Leaf فقط عملیات خود را انجام می‌دهدکلاینت بدون توجه به نوع جزء با آن ارتباط می‌گیرد. 


## 🚀 تمرین پیشنهادی واقعی (Real Practice)
### 🔧 تمرین ۱ – منوی رستوران

یک سیستم منوی رستوران بسازید که:

غذاها → Leaf

منوها → Composite

در پایان کل منو را چاپ کنید.

### 🔧 تمرین ۲ – فایل و پوشه (File System)

ساختار زیر را با Composite بسازید:
```bash 

/root
   /images
       img1.jpg
       img2.jpg
   /documents
       cv.pdf
       notes.txt

```
و متدی ایجاد کنید که کل درخت را نمایش دهد.

### 🔧 تمرین ۳ – دسته‌بندی محصولات

یک سیستم دسته‌بندی فروشگاه اینترنتی بسازید:

دسته‌ها → Composite

محصول‌ها → Leaf

متد جستجو (search) را روی کل ساختار اعمال کنید.

## منابع پیشنهادی برای مطالعه بیشتر

🌐 Refactoring.Guru – Composite Pattern

📘  Head First Design Patterns (فصل Composite)

🎥 YouTube درباره کاربرد Composite در پروژه‌های واقعی