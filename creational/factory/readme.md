<div dir="rtl" lang="fa">

# 🏭 الگوی طراحی متد کارخانه‌ای (Factory Method Pattern)

الگوی **Factory Method** یکی از مهم‌ترین الگوهای **ایجادی (Creational Patterns)** است.  
هدف آن این است که منطق ساخت شیء را از کلاس‌های اصلی جدا کند تا بتوانیم بدون تغییر کد اصلی، انواع مختلفی از اشیاء را ایجاد کنیم.



## 🎯 هدف الگو

گاهی در نرم‌افزار نیاز داریم که اشیاء مختلفی بسازیم که همگی از یک نوع پایه (interface یا abstract class) پیروی می‌کنند،  
اما هرکدام رفتار خاص خود را دارند. در چنین شرایطی به‌جای استفاده از `if/else` یا `switch`، از **Factory Method** استفاده می‌کنیم تا ساخت شیء به کلاس‌های فرزند سپرده شود.



## 🧠 چطور این الگو را عمیق یاد بگیریم؟

برای یادگیری مؤثر این الگو مراحل زیر را دنبال کنید 👇

1- ابتدا **اسلایدآموزشی (slides)** را مرور کنید تا با مفهوم و نمودار کلاس‌ها آشنا شوید.  
2- سپس فایل **`main.py`** را باز کرده و اجرا کنید تا تفاوت دو کارخانه را در خروجی ببینید.  
3- در نهایت، وارد پوشه‌ی **`real-sample/`** شوید و نمونه‌کدهای واقعی از پروژه‌های صنعتی را بررسی کنید تا ببینید **چه زمانی نیاز به تغییر نوع شیء بدون تغییر منطق اصلی داریم.**

> این سه گام کمک می‌کند تا درک کنید Factory Method چطور باعث افزایش انعطاف‌پذیری و رعایت اصل **Open/Closed Principle** می‌شود.



## 🧩 توضیح کد `main.py`

در این مثال:

1- کلاس `Creator`: متدی انتزاعی به نام `factory_method()` دارد که نوع محصول را مشخص نمی‌کند.  
2- کلاس‌های `ConcreteCreator1` و `ConcreteCreator2`: پیاده‌سازی واقعی متد کارخانه هستند و هرکدام یک نوع خاص از محصول را می‌سازند.  
3- `Product`: یک رابط (interface) برای همه‌ی محصولات است.  
4- `ConcreteProduct1` و `ConcreteProduct2`: دو نوع محصول واقعی با پیاده‌سازی متفاوت از متد `operation()`.  
5- `client_code()`: بدون اطلاع از نوع واقعی کلاس سازنده، از محصولات استفاده می‌کند.  

####  این ساختار باعث می‌شود کد کلاینت نیازی به تغییر نداشته باشد حتی اگر محصولات جدید اضافه شوند.    به این ترتیب، الگو **اصل باز-بسته (Open/Closed Principle)** را رعایت می‌کند.



####  🧰 خروجی اجرای برنامه (output.txt)
## 🚀 تمرین پیشنهادی واقعی

برای درک بهتر، این تمرین را انجام دهید 👇

### 🔹 سناریو: ساخت سیستم پرداخت در فروشگاه آنلاین

می‌خواهی سیستمی بسازی که بتواند پرداخت‌ها را از درگاه‌های مختلف (PayPal، Stripe، Zarinpal و ...) مدیریت کند.

### **تمرین:**
1- یک کلاس پایه `PaymentCreator` بساز که متد انتزاعی `factory_method()` دارد.  
2- برای هر نوع پرداخت (مثلاً `PaypalCreator`، `StripeCreator`) کلاس جدا بنویس که محصول مخصوص خودش (مثلاً `PaypalPayment`) را تولید کند.  
3- در تابع اصلی فقط با `PaymentCreator` کار کن تا در آینده بتوانی درگاه جدید اضافه کنی بدون تغییر در منطق اصلی.

🎯 **هدف:** درک مزیت Factory Method در جداسازی منطق ساخت از منطق استفاده و تسهیل گسترش سیستم.



## 📚 منابع پیشنهادی برای مطالعه بیشتر

 🌐 [Refactoring.Guru - Factory Method Pattern](https://refactoring.guru/design-patterns/factory-method)  
 📘 کتاب *Head First Design Patterns* (فصل ۴: معرفی Factory Method)  
 🎥 ویدیوهای آموزشی YouTube درباره‌ی Factory Method در پروژه‌های واقعی  



</div>




