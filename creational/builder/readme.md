<div dir="rtl" lang="fa">

# 🧱 الگوی طراحی سازنده (Builder Pattern)

الگوی **Builder** یکی از الگوهای **ایجادی (Creational Patterns)** است که برای ساخت اشیاء پیچیده به‌صورت گام‌به‌گام استفاده می‌شود.  
هدف اصلی این الگو جداسازی فرآیند ساخت (Construction) از نمایش نهایی (Representation) است تا بتوان با یک روند ساخت، چند نوع شیء مختلف تولید کرد.



## 🎯 هدف الگو

گاهی ساخت یک شیء بسیار پیچیده است و نیاز دارد که به‌صورت مرحله‌به‌مرحله قطعات مختلف آن ساخته شود.  
به‌جای ایجاد سازنده‌هایی (Constructors) با ورودی‌های زیاد، می‌توان از Builder استفاده کرد تا هر بخش شیء را جداگانه بسازد و در نهایت محصول نهایی را ارائه دهد.

مثلاً در ساخت یک گزارش، می‌توان بخش‌های **Header، Body، Footer** را جدا ساخت و در پایان آن‌ها را ترکیب کرد.



## 🧠 چطور این الگو را عمیق یاد بگیریم؟

برای یادگیری مؤثر این الگو، پیشنهاد می‌شود مراحل زیر را دنبال کنید 👇

1- ابتدا **اسلاید آموزشی (slides)** را مرور کنید تا با مفهوم و ساختار کلی آشنا شوید.  
2- سپس فایل **`main.py`** را بررسی کرده و آن را اجرا کنید تا منطق ساخت مرحله‌ای را در عمل ببینید.  
3- در نهایت، وارد پوشه‌ی **`real-sample/`** شوید و نمونه‌کدهای واقعی را ببینید تا متوجه شوید **در چه پروژه‌هایی و با چه هدفی** از این الگو استفاده می‌شود.  

> با دنبال کردن این سه مرحله، درک عمیق‌تری از نقش Builder در ایجاد اشیاء پیچیده و منعطف به‌دست خواهید آورد.



## 🧩 توضیح کد `main.py`

در این مثال:

1-کلاس  `Builder`: رابطی انتزاعی است که متدهایی برای ساخت بخش‌های مختلف محصول تعریف می‌کند (PartA, PartB, PartC).  
2- `ConcreteBuilder1`: پیاده‌سازی واقعی سازنده است که نحوه‌ی ساخت هر بخش را مشخص می‌کند و محصول نهایی را برمی‌گرداند.  
3- `Product1`: شیء نهایی است که از بخش‌های مختلف تشکیل می‌شود.  
4- `Director`: مسئول ترتیب اجرای مراحل ساخت است. (مثلاً ساخت نسخه‌ی ساده یا کامل از محصول)  
5- در تابع اصلی، کاربر (Client) از Director استفاده می‌کند تا ساخت محصولات مختلف را با یک Builder مشابه کنترل کند.  



### 🧰 خروجی اجرای برنامه (output.txt)



 این یعنی فرآیند ساخت از نوع محصول جدا شده و می‌توان بدون تغییر منطق کلی، ساختار محصول را عوض کرد.


## 🚀 تمرین پیشنهادی واقعی

برای درک عمیق‌تر، یک مثال واقعی بساز 👇  

### 🔹 ساخت سیستم سفارش خودرو

سناریو:
می‌خواهی سیستمی بسازی که بتواند خودروها را با پیکربندی‌های مختلف بسازد (مثلاً مدل پایه، لوکس، اسپرت).

**تمرین:**
1- یک `CarBuilder` طراحی کن که متدهایی برای افزودن موتور، رنگ، تایر و امکانات داخلی داشته باشد.  
2-یک `Director` بنویس که ترتیب ساخت مدل‌های مختلف خودرو را مشخص کند (مثلاً مدل اقتصادی فقط با امکانات پایه).  
3- در انتها با اجرای کد، مدل‌های مختلف خودرو را چاپ کن.  

🎯 **هدف:** یاد بگیر چطور می‌توان فرآیند ساخت را از نوع محصول جدا کرد تا بدون تغییر در منطق ساخت، محصولات جدیدی تولید شوند.



## 📚 منابع پیشنهادی برای مطالعه بیشتر

1- 🌐 [Refactoring.Guru - Builder Pattern](https://refactoring.guru/design-patterns/builder)  
2- 📘 کتاب *Head First Design Patterns* (فصل مربوط به Builder)  
3- 🎥 ویدیوهای آموزشی YouTube درباره‌ی Builder Pattern در پروژه‌های واقعی  



</div>
