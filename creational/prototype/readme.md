<div dir="rtl" lang="fa">

# 🧬 الگوی طراحی نمونه‌اولیه (Prototype Pattern)

الگوی **نمونه‌اولیه (Prototype)** یکی از الگوهای مهم **ایجادی (Creational Patterns)** است که هدف آن **کپی‌کردن اشیاء موجود بدون نیاز به وابستگی به کلاس دقیق آن‌ها** است.

در واقع، به‌جای اینکه شیء جدیدی از ابتدا بسازیم، یک نمونه‌ی اولیه (Prototype) از شیء موجود می‌سازیم و آن را کپی می‌کنیم — چه به‌صورت سطحی (shallow) و چه به‌صورت عمیق (deep).



## 🎯 هدف الگو

در موقعیت‌هایی که ساخت یک شیء پیچیده، پرهزینه یا زمان‌بر است (مثل شیئی که شامل تنظیمات زیاد یا وابستگی‌های تو در تو است)، استفاده از **Prototype** کمک می‌کند با **کپی از نمونه‌های موجود**، اشیاء جدید بسازیم بدون اجرای مجدد منطق ساخت.



## 🧠 چطور این الگو را عمیق یاد بگیریم؟

برای یادگیری مؤثر این الگو پیشنهاد می‌شود مراحل زیر را دنبال کنید 👇

1- ابتدا **اسلایدآموزشی (slides)** را مرور کنید تا با مفهوم و نمودار کلاس‌ها آشنا شوید.  
2- فایل **`main.py`** را بخوانید تا منطق ساخت و کپی اشیاء را درک کنید.  
3-  برنامه را اجرا کنید و تفاوت **کپی سطحی (shallow copy)** و **کپی عمیق (deep copy)** را مشاهده کنید.  
4- سپس در پوشه‌ی **`real-sample/`** نمونه‌ی واقعی پروژه‌ها را ببینید که در آن‌ها از این الگو استفاده شده (مثل بازی‌ها، سیستم‌های گرافیکی یا مدل‌های داده‌ای).  

####  اجرای عملی این مراحل باعث می‌شود بتوانید Prototype را در پروژه‌های واقعی خود به‌کار بگیرید.


## 🧩 توضیح کد `main.py`

در این مثال:

1- کلاس `SomeComponent` یک شیء با ویژگی‌های مختلف است که قابلیت کپی دارد.  
2- متدهای `__copy__` و `__deepcopy__` رفتار کپی سطحی و عمیق را مشخص می‌کنند.  
3- کلاس `SelfReferencingEntity` برای نمایش وابستگی‌های درونی (self-reference) استفاده می‌شود تا نشان دهد الگو چگونه از تکرار بی‌نهایت جلوگیری می‌کند.  
4- در بخش `__main__` تفاوت بین کپی سطحی و عمیق با تست‌های مختلف نمایش داده می‌شود.



####  🧰 خروجی اجرای برنامه (output.txt)

#### 🔹 در انتها، شناسه‌های حافظه (ID) چاپ می‌شوند تا مشخص شود **اشیاء در کپی عمیق جدا هستند ولی ارجاع‌های درونی درست حفظ شده‌اند.**

---

## 🚀 تمرین پیشنهادی (در پروژه واقعی)

برای درک بهتر، یک تمرین واقعی پیاده‌سازی کنید 👇

### 🧾 سناریو: ساخت قالب سند در یک نرم‌افزار گزارش‌گیری

می‌خواهی سیستمی بسازی که چندین نوع گزارش با ساختار مشابه تولید کند (مثلاً گزارش فروش روزانه، هفتگی و ماهانه).

🔹 تمرین:

1-  یک کلاس `ReportTemplate` بساز که شامل بخش‌هایی مثل `Header`، `Body` و `Footer` باشد.  
2-  از یک نمونه‌ی اصلی (prototype) از گزارش استفاده کن و برای هر نوع گزارش جدید، فقط مقادیر لازم را تغییر بده.  
3-  از متد `copy.deepcopy()` برای ساخت گزارش‌های جدا از هم استفاده کن.

🔹 هدف:
درک اینکه چطور می‌توان با Prototype از تکرار ساخت اشیاء پیچیده جلوگیری کرد و درعین‌حال استقلال داده‌ها را حفظ نمود.



## 📚 منابع پیشنهادی برای مطالعه بیشتر

1️⃣ [Refactoring.Guru - Prototype Pattern](https://refactoring.guru/design-patterns/prototype)  
2️⃣ کتاب *Head First Design Patterns*  
3️⃣ ویدیوهای آموزشی یوتیوب درباره‌ی تفاوت shallow و deep copy در پایتون

</div>
