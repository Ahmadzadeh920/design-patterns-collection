<div dir="rtl" lang="fa">

# 🧩 الگوی طراحی تک‌نمونه‌ای (Singleton Pattern)

الگوی **تک‌نمونه‌ای (Singleton)** یکی از ساده‌ترین اما پرکاربردترین **الگوهای ایجادی (Creational Patterns)** است.  
هدف این الگو اطمینان از این است که **فقط یک نمونه (Instance)** از یک کلاس در کل برنامه وجود داشته باشد، و همه‌ی بخش‌های سیستم از همان نمونه استفاده کنند.


## 🎯 هدف الگو

در برخی سیستم‌ها نیاز داریم که فقط یک شیء از یک کلاس وجود داشته باشد — مثلاً:

*- * مدیریت اتصال به دیتابیس  
*- ثبت لاگ‌های سیستم (Logging)  
*- مدیریت پیکربندی‌ها (Configuration Manager)  
*- کنترل دسترسی به منابع مشترک (Shared Resources)

الگوی Singleton این اطمینان را می‌دهد که:
*- فقط یک نمونه ساخته می‌شود.  
*- دسترسی سراسری (Global Access) به آن نمونه فراهم است.



## 🧠 چطور این الگو را عمیق یاد بگیریم؟

برای یادگیری مؤثر Singleton پیشنهاد می‌شود مراحل زیر را دنبال کنید 👇

1- ابتدا **اسلایدآموزشی (slides)** را مرور کنید تا با مفهوم و نمودار کلاس‌ها آشنا شوید.  
2-  فایل **`main.py`** را بخوانید تا مفهوم متاکلاس و مکانیزم ایجاد تنها یک نمونه را درک کنید.  
3-  کد را اجرا کنید و بررسی کنید که چرا دو متغیر مختلف (`s1` و `s2`) به همان نمونه اشاره دارند.  
4-  سپس این مفهوم را در یک پروژه واقعی پیاده‌سازی کنید (مثلاً برای کلاس Logger یا DatabaseConnector).  

#### نکته: در پایتون، Singleton را می‌توان با روش‌های مختلفی پیاده‌سازی کرد — از جمله **metaclass**، **decorator** و **module-level singleton**.  
####  در این پروژه، پیاده‌سازی متاکلاس انتخاب شده چون تمیز و پایدار است.



## 🧩 توضیح کد `main.py`

در این مثال:

1- کلاس `SingletonMeta` یک **متاکلاس (metaclass)** است که وظیفه دارد کنترل کند فقط یک نمونه از هر کلاس ایجاد شود.  
2- در متد `__call__` بررسی می‌شود که آیا قبلاً نمونه‌ای از کلاس وجود دارد یا نه.  
3- اگر نمونه وجود نداشته باشد ساخته می‌شود، در غیر این صورت همان نمونه‌ی قبلی برگردانده می‌شود.  
4- کلاس `Singleton` از این متاکلاس استفاده می‌کند تا رفتار Singleton را به ارث ببرد.  
5- در بخش `__main__` با مقایسه‌ی شناسه‌های حافظه (id) دو متغیر `s1` و `s2`، مشاهده می‌شود که هر دو به یک شیء اشاره دارند.


####  🧰 خروجی اجرای برنامه (output.txt)


#### ✅ یعنی هرچند دو شیء متفاوت ساخته شده‌اند، هر دو در واقع به **همان نمونه‌ی واحد** از کلاس Singleton اشاره دارند.



## 🚀 تمرین پیشنهادی (در پروژه واقعی)

برای درک بهتر، یک مثال واقعی بساز 👇

### 🧾 سناریو: سیستم مدیریت لاگ مرکزی (Logger)

می‌خواهی سیستمی داشته باشی که همه‌ی بخش‌های برنامه گزارش‌ها را در یک فایل مشترک بنویسند.

🔹 تمرین:

*- * یک کلاس `Logger` بساز که از SingletonMeta ارث‌بری کند.  
*- هر بار که متد `log(message)` فراخوانی می‌شود، متن را در یک فایل `app.log` بنویسد.  
*- اطمینان حاصل کن که در سراسر برنامه، فقط یک Logger وجود دارد.

🔹 هدف:
درک کاربرد واقعی Singleton در **کنترل منابع مشترک** مثل فایل‌ها، پایگاه‌داده، یا تنظیمات برنامه.



## 📚 منابع پیشنهادی برای مطالعه بیشتر

1️⃣ [Refactoring.Guru - Singleton Pattern](https://refactoring.guru/design-patterns/singleton)  
2️⃣ کتاب *Head First Design Patterns*  
3️⃣ مقاله‌های Medium درباره‌ی Singleton در پایتون و تفاوت پیاده‌سازی‌ها با متاکلاس و دکوراتور  

</div>
