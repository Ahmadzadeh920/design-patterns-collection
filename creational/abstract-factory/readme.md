
<div dir="rtl" lang="fa">

# 🏭 الگوی طراحی کارخانه‌ی انتزاعی (Abstract Factory)

الگوی **کارخانه‌ی انتزاعی** یکی از مهم‌ترین و پرکاربردترین الگوهای **ایجادی (Creational Patterns)** است.  
هدف آن ایجاد یک واسط برای ساخت گروهی از اشیاء مرتبط است، بدون اینکه نیاز باشد کلاس‌های دقیق آن اشیاء را مشخص کنیم.



## 🎯 هدف الگو

گاهی در یک سیستم چند خانواده از اشیاء وجود دارد که باید با یکدیگر سازگار باشند.  
با استفاده از Abstract Factory، می‌توان مجموعه‌ای از اشیاء مرتبط (مثلاً دکمه و چک‌باکس برای یک سیستم رابط کاربری) را بدون وابستگی به پیاده‌سازی خاص هرکدام ساخت.




## 🧠 چطور این الگو را عمیق یاد بگیریم؟

برای یادگیری مؤثر این الگو پیشنهاد می‌شود مراحل زیر را دنبال کنید 👇

1- ابتدا **اسلاید آموزشی (slides)** را مرور کنید تا با مفهوم، ساختار و منطق الگو آشنا شوید.  
2- سپس فایل **`main.py`** را بررسی کرده و آن را اجرا کنید تا رفتار عملی و خروجی الگو را ببینید.  
3- در نهایت، وارد پوشه‌ی **`real-sample/`** شوید و نمونه‌کدهای واقعی را مرور کنید تا متوجه شوید **در چه شرایطی و در چه نوع پروژه‌هایی** از این الگو استفاده می‌شود.  

> با دنبال کردن این سه مرحله، درک عمیق‌تری از هر الگو و کاربرد واقعی آن در پروژه‌های صنعتی به‌دست خواهید آورد.
---

## 🧩 توضیح کد `main.py`

این کد مثالی از **Abstract Factory Pattern** است (که Factory Method زیرمجموعه‌ای از آن محسوب می‌شود).  
در این مثال:

1- کلاس`AbstractFactory`: رابطی کلی برای ساخت اشیاء از خانواده‌های مختلف است (مثلاً ProductA و ProductB).  
2- `ConcreteFactory1` و `ConcreteFactory2`: نسخه‌های متفاوتی از محصولات را تولید می‌کنند (مثلاً نوع ۱ و نوع ۲).  
3- `AbstractProductA` و `AbstractProductB`: رابط‌های پایه برای محصولات هستند.  
4- `ConcreteProductA1`، `ConcreteProductA2`، `ConcreteProductB1` و `ConcreteProductB2`: پیاده‌سازی‌های واقعی محصولات هستند.  
5- تابع `client_code(factory)` فقط با رابط‌های انتزاعی کار می‌کند و هیچ اطلاعی از نوع دقیق کلاس‌ها ندارد.  


### 🧰 خروجی اجرای برنامه (output.txt)

### ✅ یعنی هر کارخانه مجموعه‌ی متفاوتی از محصولات سازگار تولید کرده است.


# 🚀 تمرین پیشنهادی

برای درک بهتر، یک مثال واقعی اضافه کنید:

### تولید مستندات در فرمت‌های مختلف

🔹 سناریو:
می‌خواهی سیستمی بسازی که گزارش‌ها را در قالب‌های مختلف (PDF، DOCX، HTML) تولید کند.

🔹 تمرین:

*- یک ReportFactory بساز که متدهایی برای ساخت بخش‌های مختلف گزارش (Header، Body، Footer) دارد.

*- برای هر فرمت فایل (PDF، DOCX و HTML) کارخانه‌های جداگانه بساز.

*- برنامهٔ اصلی فقط با ReportFactory کار کند.

🔹 هدف:
یاد بگیری چطور Abstract Factory به‌صورت تمیز از چندین پیاده‌سازی موازی پشتیبانی می‌کند.



# 📚 منبع پیشنهادی برای مطالعه بیشتر



2- کتاب Head First Design Patterns

3- ویدیوهای آموزشی YouTube درباره‌ی Abstract Factory و Factory Method

1-سایت  Refactoring.Guru: Factory Method
</div>